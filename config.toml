baseURL = "https://amazondigitalecuador.com/"
languageCode = "en-us"
title = "Amazon Digital Ecuador"
disableKinds = ["taxonomy", "taxonomyTerm"]
# We do redirects via Netlify's _redirects file, generated by Hugo (see "outputs" below).
disableAliases = true
